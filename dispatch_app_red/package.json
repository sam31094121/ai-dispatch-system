{
  "name": "zhaogui-ai-dispatch-all-in-one",
  "version": "1.0.0",
  "description": "兆櫃AI派單整合系統｜工程鎖死版",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check server.js && node --check shared/dispatch-engine.js && node --check shared/gemini.js && node scripts/check-dispatch-api.js",
    "check:dispatch-api": "node scripts/check-dispatch-api.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "compression": "^1.8.1",
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "helmet": "^8.1.0"
  }
}
