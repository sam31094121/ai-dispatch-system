# 後端流程狀態同步優化計畫書（2026年3月版）

*   **報告主題**：前後端狀態同步 ＆ 輕量化推播機制優化
*   **報告日期**：2026年3月23日
*   **提案／審核人**：威諺
*   **優化目標**：與前端「10色狀態視覺化」緊密對接。讓後端在每個階段（接收➔解析➔審計➔排序➔公告➔播報➔LINE➔存檔➔備份➔日誌）執行時，能**主動推播精準進度**給前端，達成「狀態即真相」的前後端一體化。

---

## 🎯 一、流程假設與對應節點

後端當前（或預期）之 10 核心自動化流水線節點如下：

| 代號 | 功能節點 | 後端對應事件／API 動作 | 狀態回報時機 |
| :---: | :--- | :--- | :--- |
| **1** | **接收** | `POST /api/report/parse` 剛接收 Request | 任務佇列載入成功時 |
| **2** | **解析** | 正則表達式、JSON 資料擷取進行中 | 解析完成/失敗前 |
| **3** | **審計** | 門檻值比對、極端值與金額勾稽檢查 | 審計判定出爐時 |
| **4** | **排序** | 調用 `sortEmployees` 被共用排序契約 | 計算完畢導出名次時 |
| **5** | **公告** | 組裝 `announce` 公告與激勵文字文案 | 文案 Ready 為止 |
| **6** | **播報** | 拋送文字至語音合成（TTS）緩衝區 | 播放器啟動時 |
| **7** | **LINE** | 生成 LINE 複製格式或推送至群組 | 點擊/發送瞬發完畢 |
| **8** | **存檔** | `fs.writeFileSync` 執行 I/O 寫入時 | 檔案寫入成功落地時 |
| **9** | **備份** | 複製到 `backups/` 異地/次要備援區 | 磁碟複製完畢時 |
| **0** | **日誌** | `fs.appendFileSync(LOG_FILE)` 終端寫入 | 日誌結案記錄完成 |

---

## 🛠️ 二、優化技術方案（AI 專家建議）

為了解決推送狀態，通常需要雙向通道。這裡提供兩種落地實作方案：

### 🔬 方案 A：Server-Sent Events (SSE) —— 【🔥 強烈推薦：免安裝依賴】
*   **原理**：利用 HTTP 長連接，由伺服器單向向瀏覽器發送事件流 `text/event-stream`。
*   **優點**：**完全不需要安裝任何 Package (`npm install`)**，純 `Node.js` 原生支持。代碼輕量，非常適合「後端單向推送狀態給前端亮燈」的情境！
*   **前端調用**：使用 HTML5 標準的 `new EventSource('/api/events')`。

### 📦 方案 B：WebSocket (Socket.io)
*   **原理**：建立 TCP 全雙工雙向數據通道。
*   **優點**：雙向互動性強、支援度廣。
*   **風險**：需要安裝 `socket.io` 依賴套件，且需改動 `server.js` 與埠號連線邏輯，架構級別較重。

---

## 📈 三、本階段預期效益
1.  **燈號百分百真實**：前端 1-10 燈號不再是模擬動畫，而是後端 CPU 剛算完、剛存完檔的「絕對真相」。
2.  **併發防護**：若多人同時輸入，後端可透過推播避免前端打架或誤判進度。

---
**簽核與執行確認**
若您確認使用 **方案 A (SSE 輕量級免裝版)**，請告知。我將為您正式升級 `server.js` 與 `index.html` 達成實體狀態連動！
